Run: python dashboard.py
This will open a localhost and open the tab and load the index.html
All files in this .zip are needed to run all of the code properly

there are multiple html files that are swapped between with the links in the navigation bar.
